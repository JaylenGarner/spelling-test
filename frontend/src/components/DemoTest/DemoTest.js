import { useState } from "react";

const DemoTest = () => {
  return (
    <form>
      <h2>Spell the word</h2>
      <input type="text" />
      <button>Submit</button>
    </form>
  );
};

export default DemoTest;
