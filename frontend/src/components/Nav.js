import { Link } from "react-router-dom";

const Nav = () => {
  return (
    <header>
      <div className="container">
        <Link to="/">Home</Link>
      </div>
    </header>
  );
};

export default Nav;
